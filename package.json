{
  "name": "javascript-warmup",
  "version": "1.0.0",
  "scripts": {
    "uglify-js": "terser src/js/*.js --compress --mangle --output dist/js/bundle.min.js"
  },
  "devDependencies": {
    "terser": "^5.10.0"
  }
}

