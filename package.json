{
  "name": "javascript-warmup",
  "version": "1.0.0",
  "scripts": {
    "uglify-js": "terser js/content_locker.js --compress --mangle --output dist/js/bundle.min.js"
  },
  "devDependencies": {
    "terser": "^5.10.0"
  }
}

